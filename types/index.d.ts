/**
 *
 * @param {string|string[]} emailAddress - String or Array of email addresses
 * @returns {boolean} true if email or all emails in array are valid else false
 */
export function validate(emailAddress: string | string[]): boolean;
